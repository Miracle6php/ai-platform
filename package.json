{
  "name": "ai-platform",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:live": "esbuild js/live-part2.js --bundle --format=esm --outfile=js/live.bundle.js --platform=browser"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@decartai/sdk": "^0.2.1",
    "mitt": "^3.0.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
